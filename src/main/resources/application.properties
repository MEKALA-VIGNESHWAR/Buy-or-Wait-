# Spring Boot Configuration
spring.application.name=buy-or-wait

# Pure command-line runner application, disable web server
spring.main.web-application-type=none

# Logging configuration
logging.level.root=INFO
logging.level.com.orchestrate.buywait=DEBUG

# Dataset and Output paths
app.dataset.dir=dataset
app.output.file=output.csv
